{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && (git diff --cached --quiet || (git commit -m \"Auto-commit: atualização automática via Claude Code\" && git push)) 2>/dev/null || true",
            "shell": "bash",
            "timeout": 30,
            "statusMessage": "Sincronizando com GitHub..."
          }
        ]
      }
    ]
  }
}
